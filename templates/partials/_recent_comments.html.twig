<div class="comments-section">
    {% for comment in recentComments %}
        <div class="comment">
            <h4>{{ comment.name }}</h4>
            <p>{{ comment.text }}</p>
            <small>Publicado el {{ comment.publishedAt|date('d F Y H:i') }}</small>
        </div>
    {% else %}
        <p>Aún no hay comentarios</p>
    {% endfor %}
</div>